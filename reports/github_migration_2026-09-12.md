# GitHub 迁移交付检查

私有仓库：https://github.com/zeng4368-netizen/ai-ecommerce-workbench

迁移快照编号：20260912T021724Z。数据包 SHA256：`d1ad7970eeb280afd6ca842881df4a1538ccc6307d7760dea04a0a5a14851ac8`，618160388 字节。

## 已验证

- 原 Skill、源码与业务文件保留目录结构，Git 禁止自动换行转换，避免跨电脑改变原文件字节。
- 1104 个初始清单文件核对通过（371 个源码/原项目文件、733 个数据文件）；后续新增的校验工具和本说明另由 Git 跟踪。
- 主库和其他 SQLite 文件通过 backup API 快照；未修改原始业务数据库，未激活待审核任务，未调用模型。
- 解密测试 6 项通过：正常恢复、拒绝覆盖、错误钥匙和路径穿越防护。
- 临时恢复目录的 `/api/health`、`/api/hub/workspace`、`/api/hub/ziniao/status` 均正常。94 项行动、18 个聊天记录、1 个内容项目、3 个数据版本、11 条采集记录可读取；未启动调度器、未恢复真实授权。
- `.env` 两份与紫鸟 CLI 的 config.json、credentials 仅在独立 Fernet 加密包内；解密钥匙不进 Git、不进 Release，保存在旧电脑 data/recovery/，最终钥匙文件已收紧为当前用户访问。

## 已知限制

- 两份 2026-06-05 历史 Excel 报告内部共 22 项 CRC/读取警告，源字节原样保留。可读内容扫描没有发现凭据样式匹配；不可读部分不能称为已完成内容审查。
- 浏览器 profile、Cookie、日志、缓存、node_modules、外部第三方克隆、独立 dashi-taskboard 项目不包含。工作台自身原模块及本地资源包含；其他工具依赖需重新安装。
- 新电脑的紫鸟终端授权、客户端账号、下载目录仍需实机检查。不会通过复制浏览器 Cookie 或绕过终端绑定实现“免登录”。
- 快照不是持续云同步；以后新增数据须重新备份，不能只 git pull 就获得新业务数据。

远端附件完整性以实际 Release 的文件大小、SHA256 和发布状态为准；不要把本地打包成功当成远端已上传完成。
