# 聊天工作区验收

- Python 全套：43 项通过（现有业务 38 项 + 新聊天 5 项）。
- Node：原分析、证据、报告渲染、行动候选及提问范围 5 组全部通过，8 个原文件校验保持一致。
- 原工作台浏览器回归：通过，商品档案 → 携带当前问题进入新聊天正常；导入/版本/原模块/内容流程仍可用，无页面脚本错误。
- 新聊天独立浏览器测试：普通聊天、连续追问、不同聊天隔离、刷新恢复、草稿保存、重命名、删除恢复、超时重试、HTML 注入防护、桌面与手机布局通过。
- 新模型功能验证使用 TEST ONLY 模拟模型；没有调用付费 API。供应商 SSE 单元测试验证真实 delta 解析、工具参数分段拼接、用量及断流/停止处理，不显示 reasoning_content。
- 实际本地服务：`http://127.0.0.1:8765/`；健康检查返回 configured，沿用服务端 DeepSeek 配置，未改密钥。
- 现有业务版本仍为 `4a8d45a4bd7a4fefaf2e68b48ac7e816`；15 个聊天/历史会话可读取；M98 实例仍保留 16 张原始图片。
- 测试截图：`data/screenshots/ai_workbench/25-chat-desktop-test.png`、`26-chat-mobile-test.png`，截图明确使用模拟数据，不当作真实模型回复展示。

说明：自动化验证覆盖交互与后端行为，不代表本次已对真实供应商做付费端到端验收。用户下次发送消息会通过原配置调用真实模型。上下文保护与能力边界见 `CHAT_GUIDE.md`。
