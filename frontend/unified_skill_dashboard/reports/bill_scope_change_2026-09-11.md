# 账单采集范围变更

依据：用户截图指定财务 → 交易 → 已结算的导出表，称为“账单”，只需此表；沿用用户明确的当月 1 日至当天时间规则。

选型：复用已检索和实测的紫鸟官方 CLI，不新增项目或依赖。GitHub 来源与取舍沿用本目录 `github_research_ziniao_2026-09-11.md`，官方 Skill 来源为 https://github.com/ziniao-open/skills 。此次仅调整现有任务范围、日期、单表校验与展示，不重复搜索或猜测新的页面选择器。

实现：新任务 required_reports=[settlement]；bill_period 使用马来西亚时区；历史无范围字段的任务继续按 orders + settlement 处理；导出及审核使用冻结的任务范围；原始平台表头和金额计算不变。账单无订单列表时明确返回不可计算。原有“每日 9 点、人工审核后启用”不变。

验证：新增月初、跨月、闰年、跨年及时区边界、手动/定时任务、账单单表导入、缺少订单数据、迁移保留旧任务、采集配方只进入账单的隔离测试。测试不连接真实店铺，不调用付费模型。实际月度账单尚未在本次修改中导出。

实测结果：专项 26 项通过；全工作台 pytest 73 项通过（83.75 秒）；两个变更 JS 文件语法检查通过；只读桌面/手机 UI 与拦截数据的账单单表 UI 检查通过，无页面脚本错误。

运行态核实：原试采目前已完成导入及 AI（由既有运行流程完成，不是本次测试调用），当前数据版本 c22ff7e4732b4a9d8b3fe5d6dd16d4f0，定时仍关闭。SQLite 备份在 data/processed/ai_workbench/workbench_before_bill_policy_20260911_141047.sqlite3。工具拒绝后台停止/重启命令，未绕过限制；PID 24252 仍运行旧后端，status 的 collection_policy 尚为空，必须手动重启工作台才能加载新采集规则。前端静态文件已可展示新版。
